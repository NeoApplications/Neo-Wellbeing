android_app {
    name: "NeoWellbeingFramework",
    defaults: ["platform_app_defaults"],
    static_libs: [
    ]
    resource_dirs: ["res"],

    srcs: [
        "aidl/**/*.aidl",
        "java/**/*.java",
        "java_real/**/*.java"
    ],

    platform_apis: true,
    privileged: true,
    certificate: "platform"
}
